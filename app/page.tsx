/**
 * Stage 0 placeholder. Real home page arrives in a later stage —
 * see docs/discovery.md for the sitemap and per-page content outline.
 */
export default function Home() {
  return (
    <main className="mx-auto max-w-3xl px-6 py-24 font-sans">
      <p className="mb-4 text-sm font-semibold tracking-widest text-blue-600 uppercase">
        Stage 0 · scaffold
      </p>
      <h1 className="text-4xl font-bold">PlatformOpsStudio</h1>
      <p className="mt-4 text-neutral-600">
        Next.js 15 rebuild in progress. See <code>docs/discovery.md</code> for the sitemap and
        per-page content outline.
      </p>
    </main>
  );
}
