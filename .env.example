# Public: safe to expose to the browser
NEXT_PUBLIC_SITE_URL=https://platformopsstudio.com

# Server-only: never expose to the browser
# Resend transactional email API key — used by the contact form endpoint (stage 6+).
RESEND_API_KEY=
# Destination inbox for contact-form submissions.
CONTACT_TO_EMAIL=ramsudarsan@gmail.com
